// 도깨비 PJ 메인 JS - main JS

// 로딩구역 호출
window.addEventListener('DOMContentLoaded',loadFn);

// 로딩구역 함수 구역/////////////////////////////
function loadFn(){
    // 로딩 호출 확인
    console.log('로딩완료!!');

    // 뷰드러운 스크롤 적용
    startSS();

} //////loadFn //////////////////////////////////